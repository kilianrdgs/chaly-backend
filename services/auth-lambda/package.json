{
  "name": "auth-lambda",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run prisma:generate && ts-node-dev --respawn --clear src/dev.ts",
    "build": "tsc",
    "deploy": "serverless deploy",
    "prisma:generate": "cp ../../prisma/schema.prisma prisma/schema.prisma && npx prisma generate"
  },
  "devDependencies": {
    "@hono/node-server": "^1.19.6",
    "@types/node": "^24.10.0",
    "esbuild": "^0.21.4",
    "npm-run-all2": "^6.2.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "hono": "^4.10.4",
    "prisma": "^6.18.0"
  }
}
